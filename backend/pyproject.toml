[project]
name = "todo"
authors = [{name = "playeriv65", email = "playeriv65@gmail.com"}]
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy",
    "pydantic",
    "uuid6",
]

[project.optional-dependencies]
dev = [
    "pytest"
]
